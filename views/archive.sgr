extends(src='templates/layout.sgr')
  block(name='title')
    title Post Archive | Modified Musings

  block(name='content')
    #archive
      .g-title
        h1 Post Archive
        h3 That's right, every single one!

      each(loop='post of contentful.fullPosts')
        include(src='/templates/_exGen.sgr')
