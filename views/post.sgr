extends(src='layout.sgr')
  
  block(name='title')
    title {{{JSON.stringify(item)}}} - Enlightened Modifications

  block(name='content')
    |{{{JSON.stringify(item)}}}
    //- h1 {{ item.title }}
    //- p(md) {{ item.body }}

    //- p.categories(md) filed under 
    //-   each(loop='category of item.category')
    //-     span: a(href='categories/{{ category }}.html') {{ category }}